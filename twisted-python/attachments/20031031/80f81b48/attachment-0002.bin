from twisted.application import service

from ratcontrol import tap, config

options = { 'pb': True,
            'web': False,
            'pbport': config.portNumber,
            'ssl': 'ssl=0' }

ser = tap.makeService(options)
application = service.Application('ratcontrol')
ser.setServiceParent(service.IServiceCollection(application))
