from twisted.web.woven import page, view, widgets

model = {'title': "Admtool Web Interface",
		 'commands': ['mkview', 'rmview', 'mkvob', 'rmvob']}

class Index(page.Page):
	templateFile = "index.html"
	templateDirectory = r'c:\share\woven\site'

	def wvfactory_menu(self, request, node, model):
		return CommandMenu()

	def wvfactory_header(self, request, node, model):
		return Header()

	def wvfactory_footer(self, request, node, model):
		return Footer()

	def wvfactory_body(self, request, node, model):
		return CommandOptions()

	def wmfactory_title(self, request):
		return "Admtool Web Interface"

	def wmfactory_commands(self, request):
		return ['mkview', 'rmview', 'lsview']

class CommandMenu(page.Page):
	templateFile = "menu.html"
	templateDirectory = r'c:\share\woven\site'

class Header(page.Page):
	templateFile = "header.html"
	templateDirectory = r'c:\share\woven\site'
	
class Footer(page.Page):
	templateFile = "footer.html"
	templateDirectory = r'c:\share\woven\site'

class CommandOptions(page.Page):
	templateFile = "body.html"
	templateDirectory = r'c:\share\woven\site'

resource = Index()
