Index: webform.py
===================================================================
--- webform.py	(revision 1661)
+++ webform.py	(working copy)
@@ -393,7 +393,16 @@
     implements(inevow.IRenderer)
 
     def rend(self, context, data):
-        return input(id=keyToXMLID(context.key), type='submit', value=data.label, name=data.name, class_="freeform-button")
+        inp = input(id=keyToXMLID(context.key), type='submit', value=data.label, 
+                    name=data.name, class_="freeform-button")
+        context.fillSlots( 'label', '')
+        context.fillSlots( 'name', data.name )
+        context.fillSlots( 'input', inp )
+        context.fillSlots( 'error', '')
+        context.fillSlots( 'description', data.description )
+        context.fillSlots( 'id', keyToXMLID(context.key) )
+        context.fillSlots( 'value', data.label)
+        return context.tag
 compy.backwardsCompatImplements(ButtonRenderer)
 
